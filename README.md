Clone the repo and install all the packages and then run (npm start / ng serve) the project.
or 
download the zip file then extract and then install all the packages and then run (npm start / ng serve) the project.
